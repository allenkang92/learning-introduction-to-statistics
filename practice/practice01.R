# 변수 a에 숫자 1 할당
# <- 는 R에서 변수에 값을 할당하는 연산자
a <- 1

# 변수 b에 숫자 1 할당
b <- 1

# 변수 a와 b의 값을 더하여 그 결과를 변수 c에 할당
# 이 경우 c는 2
# 주의: c는 R에서 벡터를 생성하는 기본 함수 이름이므로 일반적으로 변수명으로 사용하지 않는 것이 좋다.
c <- a + b

# 변수 c의 값을 출력
# 명령어 앞에 할당 연산자 없이 변수명만 작성하면 그 값이 콘솔에 출력.
# 이 코드를 실행하면 콘솔에 2 출력.
c